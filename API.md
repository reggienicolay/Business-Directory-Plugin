# Business Directory Pro REST API Reference

**Plugin Version:** 0.1.9
**API Namespace:** `bd/v1`
**Base URL:** `https://yoursite.com/wp-json/bd/v1/`

---

## Table of Contents

1. [Overview](#1-overview)
2. [Authentication](#2-authentication)
3. [Rate Limiting](#3-rate-limiting)
4. [CAPTCHA (Turnstile)](#4-captcha-turnstile)
5. [Response Formats](#5-response-formats)
6. [Businesses](#6-businesses)
7. [Submit Business](#7-submit-business)
8. [Reviews](#8-reviews)
9. [Claims](#9-claims)
10. [Badges](#10-badges)
11. [Lists](#11-lists)
12. [Collaborators](#12-collaborators)
13. [Covers](#13-covers)
14. [Feature Embeds](#14-feature-embeds)
15. [Geocode](#15-geocode)
16. [Share Tracking](#16-share-tracking)
17. [Widgets](#17-widgets)

---

## 1. Overview

The Business Directory Pro REST API follows WordPress REST API conventions. All endpoints live under the `bd/v1` namespace.

| Detail | Value |
|--------|-------|
| Base URL | `https://yoursite.com/wp-json/bd/v1/` |
| Content Type | `application/json` |
| Response Format | `WP_REST_Response` (success) or `WP_Error` (failure) |
| Authentication | WordPress cookie auth or Application Passwords |

All request parameters use standard query strings for GET requests and form-encoded or multipart bodies for POST/PUT/PATCH requests unless otherwise noted.

---

## 2. Authentication

The API uses three authentication levels depending on the endpoint.

| Level | Description | How |
|-------|-------------|-----|
| Public | No authentication required. | Send the request directly with no credentials. |
| User | Requires a logged-in WordPress user session. | Send the WordPress cookie (`_wpnonce` + session cookie) or use an Application Password via HTTP Basic Auth. |
| Admin | Requires specific WordPress capabilities. | Same mechanism as User, but the authenticated account must hold `bd_manage_claims` or `manage_options` capabilities. |

**Application Password example:**

```bash
curl -u "username:xxxx xxxx xxxx xxxx xxxx xxxx" \
  https://yoursite.com/wp-json/bd/v1/lists/my-lists
```

**Cookie auth example (browser/AJAX):**

Include the `X-WP-Nonce` header with a valid nonce generated by `wp_create_nonce('wp_rest')`.

---

## 3. Rate Limiting

Rate limits are enforced per IP address using WordPress transients.

| Endpoint | Limit | Window |
|----------|-------|--------|
| `POST /claim` | 3 requests | Per hour |
| `POST /claims/grant` | 30 requests per admin | Per 10 minutes |
| `POST /submit-business` | 3 requests | Per hour |
| `POST /submit-review` | 5 requests | Per hour |
| `POST /lists` | 10 requests | Per hour |
| `POST /share/track` | Daily limits per type | Per day |

When a rate limit is exceeded the API returns HTTP 429:

```json
{
  "code": "rate_limit_exceeded",
  "message": "Too many requests. Please try again later.",
  "data": { "status": 429 }
}
```

---

## 4. CAPTCHA (Turnstile)

Cloudflare Turnstile verification is available as an optional layer on form submission endpoints.

**Setup:**

Store the following values in `wp_options`:

| Option Key | Description |
|------------|-------------|
| `bd_turnstile_site_key` | Cloudflare Turnstile site key |
| `bd_turnstile_secret_key` | Cloudflare Turnstile secret key |

**Usage:**

When both keys are configured, include the `turnstile_token` parameter with form submissions. The server validates the token against the Cloudflare API before processing the request.

**Applicable endpoints:**

- `POST /submit-business`
- `POST /submit-review`
- `POST /claim`

If Turnstile is enabled and the token is missing or invalid, the API returns a validation error.

---

## 5. Response Formats

### Success Response

```json
{
  "success": true,
  "message": "Operation completed.",
  "data": { }
}
```

### Paginated Response

```json
{
  "businesses": [],
  "total": 150,
  "pages": 8,
  "page": 1,
  "per_page": 20
}
```

### Error Response (WP_Error)

```json
{
  "code": "error_code",
  "message": "Human-readable error.",
  "data": { "status": 400 }
}
```

**Common error codes:**

| Code | HTTP Status | Meaning |
|------|-------------|---------|
| `rest_forbidden` | 403 | Insufficient permissions |
| `rate_limit_exceeded` | 429 | Rate limit hit |
| `missing_param` | 400 | Required parameter missing |
| `invalid_param` | 400 | Parameter validation failed |
| `not_found` | 404 | Resource does not exist |

---

## 6. Businesses

### GET /businesses

Search and filter business listings with optional geolocation support.

**Auth:** Public

**Parameters:**

| Parameter | Type | Required | Default | Description |
|-----------|------|----------|---------|-------------|
| `lat` | number | No | -- | Latitude for distance calculation |
| `lng` | number | No | -- | Longitude for distance calculation |
| `radius_km` | number | No | -- | Search radius in kilometers |
| `categories` | string | No | -- | Comma-separated category IDs |
| `areas` | string | No | -- | Comma-separated area IDs |
| `tags` | string | No | -- | Comma-separated tag IDs |
| `price_level` | string | No | -- | Price level: `$`, `$$`, `$$$`, `$$$$` |
| `min_rating` | number | No | -- | Minimum average rating (0-5) |
| `open_now` | boolean | No | -- | Filter for businesses currently open |
| `q` | string | No | -- | Keyword search query |
| `sort` | string | No | `distance` | Sort order: `distance`, `rating`, `newest`, `name` |
| `page` | integer | No | `1` | Page number |
| `per_page` | integer | No | `20` | Results per page (max 50) |

**Response fields:**

| Field | Type | Description |
|-------|------|-------------|
| `businesses` | array | Array of business objects |
| `businesses[].id` | integer | WordPress post ID |
| `businesses[].title` | string | Business name |
| `businesses[].slug` | string | URL slug |
| `businesses[].excerpt` | string | Short description |
| `businesses[].permalink` | string | Full URL to the listing |
| `businesses[].featured_image` | string | Featured image URL |
| `businesses[].rating` | number | Average rating (0-5) |
| `businesses[].review_count` | integer | Total number of approved reviews |
| `businesses[].price_level` | string | Price level indicator |
| `businesses[].categories` | array | Array of `{id, name, slug}` objects |
| `businesses[].areas` | array | Array of `{id, name, slug}` objects |
| `businesses[].tags` | array | Array of `{id, name, slug}` objects |
| `businesses[].location` | object | `{lat, lng, address, city, state, zip}` |
| `businesses[].phone` | string | Phone number |
| `businesses[].is_open_now` | boolean | Whether the business is currently open |
| `businesses[].distance` | object | `{km, mi, display}` -- present when lat/lng provided |
| `total` | integer | Total matching businesses |
| `pages` | integer | Total number of pages |
| `page` | integer | Current page number |
| `per_page` | integer | Results per page |
| `bounds` | object | `{north, south, east, west}` map boundaries |
| `available_tags` | array | Tags with counts derived from results |
| `filters_applied` | object | Summary of currently active filters |

**Caching:** Results are cached for 5 minutes using a WordPress transient keyed on the filter parameter hash.

**Example request:**

```bash
curl "https://yoursite.com/wp-json/bd/v1/businesses?lat=37.68&lng=-121.77&radius_km=10&sort=rating&per_page=5"
```

**Example response:**

```json
{
  "businesses": [
    {
      "id": 42,
      "title": "Valley Oak Bistro",
      "slug": "valley-oak-bistro",
      "excerpt": "Farm-to-table dining in the heart of downtown.",
      "permalink": "https://yoursite.com/places/valley-oak-bistro/",
      "featured_image": "https://yoursite.com/wp-content/uploads/2025/06/bistro.jpg",
      "rating": 4.8,
      "review_count": 23,
      "price_level": "$$",
      "categories": [
        { "id": 5, "name": "Restaurants", "slug": "restaurants" }
      ],
      "areas": [
        { "id": 12, "name": "Downtown", "slug": "downtown" }
      ],
      "tags": [
        { "id": 30, "name": "Outdoor Seating", "slug": "outdoor-seating" }
      ],
      "location": {
        "lat": 37.6819,
        "lng": -121.7680,
        "address": "123 Main St",
        "city": "Pleasanton",
        "state": "CA",
        "zip": "94566"
      },
      "phone": "(925) 555-0123",
      "is_open_now": true,
      "distance": {
        "km": 0.5,
        "mi": 0.31,
        "display": "0.3 mi"
      }
    }
  ],
  "total": 1,
  "pages": 1,
  "page": 1,
  "per_page": 5,
  "bounds": {
    "north": 37.77,
    "south": 37.59,
    "east": -121.67,
    "west": -121.87
  },
  "available_tags": [
    { "id": 30, "name": "Outdoor Seating", "slug": "outdoor-seating", "count": 1 }
  ],
  "filters_applied": {
    "radius_km": 10,
    "sort": "rating"
  }
}
```

---

### GET /filters

Retrieve filter metadata including available categories, areas, tags, and price levels.

**Auth:** Public

**Parameters:** None

**Response:** An object containing arrays of available filter options with their respective counts.

---

## 7. Submit Business

### POST /submit-business

Submit a new business listing for admin review.

**Auth:** Public
**Rate Limit:** 3 requests per hour
**CAPTCHA:** Optional (Turnstile)

**Parameters:**

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `title` | string | Yes | Business name |
| `description` | string | Yes | Business description |
| `category` | integer | No | Category term ID |
| `address` | string | No | Street address |
| `city` | string | No | City name |
| `phone` | string | No | Phone number |
| `website` | string | No | Website URL |
| `submitter_name` | string | No | Contact name of the submitter |
| `submitter_email` | string | No | Contact email of the submitter |
| `turnstile_token` | string | No | Cloudflare Turnstile CAPTCHA token |
| `photos[]` | file | No | One or more photo uploads (multipart) |

**Example request:**

```bash
curl -X POST "https://yoursite.com/wp-json/bd/v1/submit-business" \
  -F "title=Sunrise Coffee Roasters" \
  -F "description=Small-batch artisan coffee roasted locally." \
  -F "category=8" \
  -F "address=456 Elm Ave" \
  -F "city=Livermore" \
  -F "phone=(925) 555-0456" \
  -F "website=https://sunrisecoffee.example.com" \
  -F "submitter_name=Jane Doe" \
  -F "submitter_email=jane@example.com"
```

**Response:**

```json
{
  "success": true,
  "message": "Your business has been submitted for review.",
  "business_id": 123
}
```

---

## 8. Reviews

### POST /submit-review

Submit a review for a business listing.

**Auth:** Public
**Rate Limit:** 5 requests per hour
**CAPTCHA:** Optional (Turnstile)
**Duplicate Prevention:** The server checks for an existing approved or pending review from the same user account or email address and rejects duplicates.

**Parameters:**

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `business_id` | integer | Yes | ID of the business to review |
| `rating` | integer | Yes | Star rating from 1 to 5 |
| `content` | string | Yes | Review body text (10-5000 characters) |
| `author_name` | string | Conditional | Required if the user is not logged in |
| `author_email` | string | Conditional | Required if the user is not logged in |
| `title` | string | No | Review title (max 200 characters) |
| `bd_display_name` | string | No | Custom display name (logged-in users only) |
| `turnstile_token` | string | No | Cloudflare Turnstile CAPTCHA token |
| `photos[]` | file | No | Up to 3 images, 5 MB each. Accepted types: JPEG, PNG, GIF, WebP |

**Example request:**

```bash
curl -X POST "https://yoursite.com/wp-json/bd/v1/submit-review" \
  -F "business_id=42" \
  -F "rating=5" \
  -F "content=Absolutely wonderful experience. The staff was friendly and the food was outstanding." \
  -F "author_name=John Smith" \
  -F "author_email=john@example.com" \
  -F "title=Best dinner in town"
```

**Response:**

```json
{
  "success": true,
  "message": "Your review has been submitted and is awaiting moderation."
}
```

---

## 9. Claims

### POST /claim

Submit a business ownership claim.

**Auth:** Public
**Rate Limit:** 3 requests per hour
**CAPTCHA:** Optional (Turnstile)

**Parameters:**

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `business_id` | integer | Yes | ID of the business to claim |
| `claimant_name` | string | Yes | Full name of the claimant |
| `claimant_email` | string | Yes | Email address of the claimant |
| `claimant_phone` | string | No | Phone number of the claimant |
| `relationship` | string | No | Relationship to the business: `owner`, `manager`, or `employee` |
| `message` | string | No | Supporting message for the claim |
| `turnstile_token` | string | No | Cloudflare Turnstile CAPTCHA token |
| `proof_files[]` | file | No | Up to 5 proof-of-ownership documents (multipart) |

**Example request:**

```bash
curl -X POST "https://yoursite.com/wp-json/bd/v1/claim" \
  -F "business_id=42" \
  -F "claimant_name=Maria Garcia" \
  -F "claimant_email=maria@example.com" \
  -F "claimant_phone=(925) 555-0789" \
  -F "relationship=owner" \
  -F "message=I have owned this restaurant since 2019."
```

**Response:**

```json
{
  "success": true,
  "message": "Your claim has been submitted.",
  "claim_id": 45
}
```

---

### GET /claims

Retrieve pending business ownership claims.

**Auth:** Admin (`bd_manage_claims` or `manage_options`)

**Parameters:**

| Parameter | Type | Default | Description |
|-----------|------|---------|-------------|
| `status` | string | `pending` | Filter claims by status |

**Response:** An array of claim objects matching the requested status.

---

### POST /claims/{id}/approve

Approve a pending claim.

**Auth:** Admin (`bd_manage_claims` or `manage_options`)

**Parameters:**

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `notes` | string | No | Internal notes about the approval |

**Side effects:**

- Creates a WordPress user account for the claimant if one does not exist.
- Assigns the `business_owner` role to the user.
- Links the business post to the new owner.
- Sends a welcome email to the claimant with account details.

---

### POST /claims/{id}/reject

Reject a pending claim.

**Auth:** Admin (`bd_manage_claims` or `manage_options`)

**Parameters:**

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `notes` | string | No | Internal notes or reason for rejection |

**Side effects:**

- Sends a rejection notification email to the claimant.

---

### POST /claims/grant

**In-field grant access.** Authorise a known business owner (or marketing contact) to manage a business listing without making them complete the public `/claim` form. Designed for directory managers who meet owners in person.

Written as a single code path shared by three UI entry points:

1. **"Business Access" meta box** on the `bd_business` edit screen
2. **"Grant Access" row action** on the businesses list table
3. **🔑 Grant Access admin-bar node** on the public single-business page (so a directory manager on a phone can grant access directly from the business's public URL)

**Auth:** Admin (`bd_manage_claims` or `manage_options`)

**Parameters:**

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `business_id` | integer | Yes | ID of the `bd_business` post |
| `email` | string | Yes | Email address of the user to grant access to. If the user doesn't exist, an account is created. |
| `name` | string | No | Full name. Used only when creating a new account. |
| `phone` | string | No | Contact phone (stored on the claim row) |
| `relationship` | string | No | `owner` (default), `manager`, `staff`, or `other`. Multiple users may be granted per business — e.g. one `owner` plus one `manager`. |
| `note` | string | No | Internal admin note stored in the claim row's `admin_notes` column. Prefixed with "Granted in-field by {admin name}:" |
| `send_welcome` | boolean | No | Default `true`. Email a welcome message with login details to newly created users. Existing users are never sent a welcome email. |

**Response (new grant):**

```json
{
  "success": true,
  "already": false,
  "business_id": 123,
  "user_id": 456,
  "claim_id": 78,
  "created_user": true,
  "relationship": "owner",
  "message": "New user created and granted access."
}
```

**Response (user already has access):**

Idempotent — granting the same user access to the same business returns success without creating a duplicate claim row:

```json
{
  "success": true,
  "already": true,
  "business_id": 123,
  "user_id": 456,
  "claim_id": 78,
  "created_user": false,
  "relationship": "owner",
  "message": "User already has approved access to this business."
}
```

**Side effects:**

- **New users:** a WordPress account is created with the `business_owner` role and a 16-character random password. On DB failure after creation, the orphan account is deleted (rollback).
- **Existing users:** `business_owner` role is added (never replaces existing roles; admins and directory managers are never demoted).
- An approved row is inserted into `wp_bd_claim_requests` with `status=approved`, `reviewed_by` set to the acting admin, and an audit-trail entry in `admin_notes`.
- When `relationship=owner` AND no existing primary owner exists, the `bd_claimed_by` post meta is set. Secondary grants (e.g. a manager) never overwrite an existing primary owner.
- Fires `do_action( 'bd_claim_approved', $claim_id, $business_id, $user_id )` for companion-plugin compatibility.
- Fires `do_action( 'bd_access_granted', $claim_id, $business_id, $user_id, $relationship, $granted_by )` for plugins that want to differentiate in-field grants from form-approved claims.

**Example request:**

```bash
curl -X POST "https://yoursite.com/wp-json/bd/v1/claims/grant" \
  -H "X-WP-Nonce: <nonce>" \
  -H "Content-Type: application/json" \
  --cookie-jar cookies.txt \
  -d '{
    "business_id": 123,
    "email": "owner@example.com",
    "name": "Jane Doe",
    "relationship": "owner",
    "note": "Met at Livermore Farmers Market"
  }'
```

---

### GET /businesses/{id}/access

List all users with approved access to a business. Used by the Grant Access modal to render the "People with access" list.

**Auth:** Admin (`bd_manage_claims` or `manage_options`)

**Response:**

```json
{
  "business_id": 123,
  "users": [
    {
      "claim_id": 78,
      "user_id": 456,
      "display_name": "Jane Doe",
      "email": "jane@example.com",
      "phone": "(925) 555-0123",
      "relationship": "owner",
      "granted_at": "2026-04-08 14:32:10",
      "granted_by": 1,
      "admin_notes": "Granted in-field by Nicole: Met at Farmers Market",
      "is_primary": true,
      "user_missing": false
    }
  ]
}
```

**Notes:**

- `is_primary` indicates the user stored in the business's `bd_claimed_by` post meta (the "primary owner" for backward compatibility).
- `user_missing` is `true` if the associated WP user account was deleted but the claim row still exists — the UI should show a warning and offer revocation.
- Response contains PII (email, phone) and internal admin notes. Always gated by the `bd_manage_claims` capability.

---

### POST /claims/{id}/revoke

Revoke a previously granted approved claim. Distinct from `/claims/{id}/reject`, which only operates on `pending` rows.

**Auth:** Admin (`bd_manage_claims` or `manage_options`)

**Parameters:**

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `note` | string | No | Reason appended to the claim row's `admin_notes` (audit trail) |

**Response:**

```json
{
  "success": true,
  "business_id": 123,
  "user_id": 456,
  "claim_id": 78,
  "was_primary": true,
  "new_primary_id": 457,
  "message": "Access revoked."
}
```

**Side effects:**

- The claim row's `status` is flipped to `revoked`. The existing `admin_notes` (grant audit trail) is preserved; the revoke reason is appended as a new line.
- The WP user's `business_owner` role is **not** removed — they may still own other businesses. Authorization is enforced by the claim row's `status`, so flipping it to `revoked` is sufficient to cut off access to this business.
- **Primary owner reassignment:** if the revoked user was the current primary owner (`bd_claimed_by`), another remaining approved owner is promoted (preferring one with `relationship=owner`). If nobody remains, all primary-owner meta keys are cleared and the public claim form becomes available again for this business.
- Fires `do_action( 'bd_access_revoked', $claim_id, $business_id, $user_id, $revoked_by, $new_primary_id )`.

---

## 10. Badges

### GET /badge/{id}

Generate an embeddable business rating badge as an image.

**Auth:** Public

**Parameters:**

| Parameter | Type | Default | Description |
|-----------|------|---------|-------------|
| `style` | string | `rating` | Badge style: `rating`, `reviews`, or `featured` |
| `format` | string | `svg` | Output format: `svg` or `png` |
| `size` | string | `medium` | Size preset: `small` (150x60), `medium` (200x80), `large` (280x100) |

**Response:** Direct image output with the header `Cache-Control: public, max-age=3600`.

**Example request:**

```bash
curl "https://yoursite.com/wp-json/bd/v1/badge/42?style=rating&format=svg&size=large"
```

---

### GET /badge/{id}/code

Get the HTML embed code for a business badge.

**Auth:** Public

**Parameters:**

| Parameter | Type | Default | Description |
|-----------|------|---------|-------------|
| `style` | string | `rating` | Badge style: `rating`, `reviews`, or `featured` |
| `size` | string | `medium` | Size preset: `small`, `medium`, `large` |

**Response:**

```json
{
  "code": "<a href='https://yoursite.com/places/valley-oak-bistro/'><img src='https://yoursite.com/wp-json/bd/v1/badge/42?style=rating' alt='Valley Oak Bistro Rating' /></a>",
  "badge_url": "https://yoursite.com/wp-json/bd/v1/badge/42?style=rating",
  "permalink": "https://yoursite.com/places/valley-oak-bistro/"
}
```

---

## 11. Lists

### GET /lists

Browse public lists.

**Auth:** Public

**Parameters:**

| Parameter | Type | Default | Description |
|-----------|------|---------|-------------|
| `page` | integer | `1` | Page number |
| `per_page` | integer | `12` | Results per page |
| `orderby` | string | `updated_at` | Sort field |
| `featured` | boolean | -- | When true, return only featured lists |
| `search` | string | -- | Search query |

---

### POST /lists

Create a new list.

**Auth:** User
**Rate Limit:** 10 requests per hour

**Parameters:**

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `title` | string | Yes | List title |
| `description` | string | No | List description |
| `visibility` | string | No | Visibility level: `private`, `public`, or `unlisted` |

**Example request:**

```bash
curl -X POST "https://yoursite.com/wp-json/bd/v1/lists" \
  -H "X-WP-Nonce: abc123" \
  -H "Cookie: wordpress_logged_in_xxx=..." \
  -H "Content-Type: application/json" \
  -d '{"title": "Best Brunch Spots", "description": "My favorite weekend brunch places.", "visibility": "public"}'
```

**Response:**

```json
{
  "success": true,
  "message": "List created.",
  "data": {
    "id": 78,
    "title": "Best Brunch Spots",
    "slug": "best-brunch-spots",
    "visibility": "public"
  }
}
```

---

### GET /lists/my-lists

Get all lists belonging to the current authenticated user.

**Auth:** User

**Response:** An array of the user's lists with metadata.

---

### GET /lists/{id}

Get a single list by numeric ID or slug.

**Auth:** Public (respects visibility settings -- private lists require ownership)

**Side effects:** Increments the view count for the list.

---

### PUT /lists/{id}

Update list metadata.

**Auth:** List owner

**Parameters:**

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `title` | string | No | Updated title |
| `description` | string | No | Updated description |
| `visibility` | string | No | `private`, `public`, or `unlisted` |
| `cover_image_id` | integer | No | Attachment ID for a cover image |

---

### DELETE /lists/{id}

Delete a list permanently.

**Auth:** List owner

---

### POST /lists/{list_id}/items

Add a business to a list.

**Auth:** List owner or collaborator

**Parameters:**

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `business_id` | integer | Yes | ID of the business to add |
| `note` | string | No | Optional note about the business |

---

### DELETE /lists/{list_id}/items/{business_id}

Remove a business from a list.

**Auth:** List owner or collaborator

---

### PUT /lists/{list_id}/items/{business_id}

Update the note on a list item.

**Auth:** List owner or collaborator

**Parameters:**

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `note` | string | No | Updated note text |

---

### POST /lists/{list_id}/reorder

Reorder the items in a list.

**Auth:** List owner

**Parameters:**

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `order` | array | Yes | Ordered array of business IDs representing the new sort order |

---

### POST /lists/quick-save

Quickly add a business to an existing list or create a new list in one step.

**Auth:** User

**Parameters:**

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `business_id` | integer | Yes | ID of the business to save |
| `list_id` | integer | No | Existing list ID to add the business to |
| `new_list` | string | No | Title for a new list to create and add the business to |

Provide either `list_id` or `new_list`, not both.

---

### GET /lists/containing/{business_id}

Check which of the current user's lists already contain a specific business.

**Auth:** User

**Response:**

```json
{
  "list_ids": [12, 45, 78],
  "count": 3
}
```

---

### POST /lists/{list_id}/share

Track a list share event for gamification purposes.

**Auth:** User

**Parameters:**

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `platform` | string | Yes | Sharing platform identifier |

---

### GET /lists/{list_id}/share-data

Get share statistics for a list.

**Auth:** Public

**Response:** An object containing share counts broken down by platform.

---

### POST /lists/{id}/follow

Follow a list to receive updates.

**Auth:** User

---

### POST /lists/{id}/unfollow

Unfollow a previously followed list.

**Auth:** User

---

## 12. Collaborators

### GET /lists/{id}/invite

Get the invite link for a list.

**Auth:** List owner

**Response:**

```json
{
  "url": "https://yoursite.com/lists/best-brunch-spots/?invite=abc123token",
  "token": "abc123token",
  "mode": "auto"
}
```

---

### POST /lists/{id}/invite/regenerate

Regenerate the invite link, invalidating the previous one.

**Auth:** List owner

---

### PUT /lists/{id}/invite/settings

Update the invite acceptance mode.

**Auth:** List owner

**Parameters:**

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `mode` | string | Yes | Invite mode: `auto`, `approval`, or `disabled` |

---

### POST /lists/join

Join a list using an invite link.

**Auth:** User

**Parameters:**

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `slug` | string | Yes | List slug from the invite URL |
| `token` | string | Yes | Invite token from the invite URL |

---

### GET /lists/{id}/collaborators

Get active and pending collaborators for a list.

**Auth:** List owner

**Response:**

```json
{
  "collaborators": [],
  "pending": [],
  "count": 3,
  "pending_count": 1,
  "max_allowed": 20,
  "invite_mode": "auto",
  "can_add_more": true
}
```

---

### POST /lists/{id}/collaborators

Add a collaborator directly to a list.

**Auth:** List owner

**Parameters:**

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `user_id` | integer | Yes | WordPress user ID to add |
| `role` | string | No | Collaborator role: `contributor` (default) or `editor` |

---

### DELETE /lists/{id}/collaborators/{user_id}

Remove a collaborator from a list.

**Auth:** List owner

---

### POST /lists/{id}/leave

Leave a collaborative list you are a member of.

**Auth:** User (must be a collaborator on the list)

---

### GET /lists/{id}/requests

Get pending join requests for a list.

**Auth:** List owner

---

### POST /lists/{id}/requests/{user_id}/approve

Approve a pending join request.

**Auth:** List owner

---

### POST /lists/{id}/requests/{user_id}/deny

Deny a pending join request.

**Auth:** List owner

---

### GET /users/me/collaborative-lists

Get all lists the current user collaborates on (not lists they own).

**Auth:** User

---

### GET /users/me/list-notifications

Get the current user's list-related notifications.

**Auth:** User

**Response:**

```json
{
  "notifications": [],
  "unread_count": 4
}
```

---

### POST /users/me/list-notifications/{id}/read

Mark a single notification as read.

**Auth:** User

---

### POST /users/me/list-notifications/read-all

Mark all of the current user's notifications as read.

**Auth:** User

---

### GET /users/search

Search for WordPress users by name or email. Intended for the collaborator-add interface.

**Auth:** User

**Parameters:**

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `search` | string | Yes | Search query (minimum 2 characters) |
| `list_id` | integer | No | Exclude users already collaborating on this list |

---

### GET /users/me/recent-collaborators

Get users the current user has frequently collaborated with.

**Auth:** User

---

## 13. Covers

### POST /lists/{id}/cover

Upload a list cover image with optional cropping data.

**Auth:** User (list owner)
**Content-Type:** `multipart/form-data`

**Parameters:**

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `cropped` | file | Yes | Cropped image file |
| `original` | file | No | Original uncropped image file |
| `crop_data` | JSON string | No | Crop parameters: `{x, y, width, height, zoom, rotation}` |

---

### PATCH /lists/{id}/cover

Re-crop an existing cover image using new crop parameters.

**Auth:** User (list owner)

**Parameters:**

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `cropped` | file | Yes | New cropped image file |
| `crop_data` | JSON string | No | Updated crop parameters: `{x, y, width, height, zoom, rotation}` |

---

### DELETE /lists/{id}/cover

Remove the cover image from a list.

**Auth:** User (list owner)

---

### POST /lists/{id}/cover/video

Set a video as the list cover using a YouTube or Vimeo URL.

**Auth:** User (list owner)

**Parameters:**

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `video_url` | string | Yes | Full YouTube or Vimeo video URL |

---

### POST /lists/{id}/cover/video/thumbnail

Upload a custom thumbnail image for a video cover.

**Auth:** User (list owner)

**Parameters:** Multipart file upload for the thumbnail image.

---

### GET /lists/{id}/cover

Get the current cover data for a list, including image URLs and crop metadata.

**Auth:** Public

---

### POST /cover/parse-video

Parse a video URL and return platform metadata.

**Auth:** User

**Parameters:**

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `url` | string | Yes | YouTube or Vimeo video URL |

**Response:**

```json
{
  "platform": "youtube",
  "id": "dQw4w9WgXcQ",
  "thumbnail_url": "https://img.youtube.com/vi/dQw4w9WgXcQ/maxresdefault.jpg",
  "embed_url": "https://www.youtube.com/embed/dQw4w9WgXcQ"
}
```

---

## 14. Feature Embeds

### GET /feature

Get featured business data for cross-site embedding.

**Auth:** Public
**CORS:** Validates the request origin against a whitelist of allowed domains.

**Parameters:**

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `ids` | string | Yes | Comma-separated list of business post IDs |

**Response:**

```json
{
  "businesses": [],
  "count": 3,
  "source": "yoursite.com"
}
```

---

### GET /feature/search

Search businesses for embed selection in external tools.

**Auth:** Public

**Parameters:**

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `q` | string | No | Search query |
| `category` | integer | No | Filter by category term ID |
| `per_page` | integer | No | Results per page (default 20, max 50) |

**Response:**

```json
{
  "businesses": [],
  "categories": [],
  "total": 42
}
```

---

## 15. Geocode

### GET /geocode

Forward geocode an address string to geographic coordinates.

**Auth:** Public

**Parameters:**

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `address` | string | Yes | Address to geocode |

**Response:**

```json
{
  "lat": 37.6819,
  "lng": -121.7680,
  "address": "123 Main St, Pleasanton, CA 94566"
}
```

---

### GET /geocode/reverse

Reverse geocode geographic coordinates to a human-readable address.

**Auth:** Public

**Parameters:**

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `lat` | number | Yes | Latitude |
| `lng` | number | Yes | Longitude |

**Response:** An object with address components derived from the coordinates.

---

## 16. Share Tracking

### POST /share/track

Track a social share event and award gamification points.

**Auth:** Public

**Daily limits per user/IP:**

| Share Type | Daily Limit |
|------------|-------------|
| `business` | 3 |
| `review` | 1 |
| `badge` | 1 |
| `profile` | 1 |

**Parameters:**

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `type` | string | Yes | Share type: `business`, `review`, `badge`, or `profile` |
| `object_id` | mixed | Yes | ID of the shared object |
| `platform` | string | Yes | Platform: `facebook`, `linkedin`, `nextdoor`, `email`, or `copy_link` |

**Example request:**

```bash
curl -X POST "https://yoursite.com/wp-json/bd/v1/share/track" \
  -H "Content-Type: application/json" \
  -d '{"type": "business", "object_id": 42, "platform": "facebook"}'
```

**Response:**

```json
{
  "success": true,
  "points_awarded": 10,
  "limit_reached": false,
  "remaining_today": 2,
  "message": "Share tracked! You earned 10 points."
}
```

---

### GET /share/stats

Get aggregate share statistics for a specific object.

**Auth:** Public

**Parameters:**

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `type` | string | Yes | Object type: `business`, `review`, `badge`, or `profile` |
| `object_id` | mixed | Yes | ID of the object |

**Response:**

```json
{
  "total": 8,
  "platforms": {
    "facebook": 5,
    "linkedin": 3
  }
}
```

---

### GET /share/user-stats

Get the current user's share statistics and remaining daily limits.

**Auth:** User

**Response:**

```json
{
  "total_shares": 27,
  "total_points": 270,
  "remaining_today": {
    "business": 2,
    "review": 1,
    "badge": 0,
    "profile": 1
  }
}
```

---

## 17. Widgets

### GET /widget/embed.js

Serve the embeddable widget JavaScript file for use on external sites.

**Auth:** Public
**Response Content-Type:** `application/javascript`
**Cache:** `Cache-Control: public, max-age=3600` (1 hour)
**CORS:** `Access-Control-Allow-Origin: *`

Available widget styles: `compact`, `carousel`, `list`.
Available themes: `light`, `dark`.

---

### GET /widget/data/{business_id}

Get structured widget data for a business listing.

**Auth:** Public
**Domain Whitelist:** The server validates the `Referer` header against an allowed domain list.

**Parameters:**

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `business_id` | integer | Yes | Business post ID (URL path parameter) |
| `reviews` | integer | No | Number of reviews to include in the response |

**Response:**

```json
{
  "business": {},
  "rating": 4.8,
  "reviews": [],
  "review_url": "https://yoursite.com/places/valley-oak-bistro/#reviews",
  "site_url": "https://yoursite.com",
  "site_name": "Your Directory"
}
```

---

### POST /widget/click

Track a click interaction on an embedded widget.

**Auth:** Public

**Parameters:**

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `business_id` | integer | Yes | Business post ID |
| `action` | string | Yes | Interaction type: `review` or `view` |
